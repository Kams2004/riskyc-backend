package com.fashion.Riskyc.dto.response;

/** A frozen name/phone pair on a packaging-confirmation message — see entity.DeliveryContactSnapshot. */
public record DeliveryContactSnapshotResponse(String name, String phone) {
}
